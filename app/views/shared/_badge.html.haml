%div{ class: define_badge_column }
  = image_tag badge.image_url
  .badge-info-inside
    %h5.badge-name
      = badge.name
    %p.badge-description
      = badge.description
    = link_to 'Details', badge_path(badge), class: 'badge-details-link'
