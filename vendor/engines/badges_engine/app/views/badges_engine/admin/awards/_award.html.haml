%tr
  %td= award.title
  %td= award.description
  %td= link_to 'Show', admin_award_path(award)
  %td= link_to 'Edit', edit_admin_award_path(award)
  %td
    = link_to 'Destroy', admin_award_path(award), method: :delete,
      data: { confirm: 'Are you sure?' }
