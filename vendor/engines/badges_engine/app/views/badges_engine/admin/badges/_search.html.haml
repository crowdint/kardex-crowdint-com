= form_tag(badges_engine.admin_badges_path, :method => "get", id: "search-form") do
  %p Search
  = text_field_tag :search, params[:search], class: "form-control"
  = button_tag "Search", :name => nil do
    .search-glass
