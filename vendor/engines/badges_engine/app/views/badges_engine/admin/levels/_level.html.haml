%tr
  %td= show_badge(level.badge)
  %td= level.tier
  %td= level.badge_alias
  %td= link_to 'Show', admin_level_path(level)
  %td= link_to 'Edit', edit_admin_level_path(level)
  %td
    = link_to 'Destroy', admin_level_path(level), method: :delete,
      data: { confirm: 'Are you sure?' }
